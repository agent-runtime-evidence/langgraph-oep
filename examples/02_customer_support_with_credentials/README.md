# Customer Support With Credentials

Captures a scoped credential lifetime for a support-agent profile read.

Key fields: `scoped_credential_lifetime`, `decision_id.identity`, and revocation-style `decision_id.cache.invalidation_event_id`.
