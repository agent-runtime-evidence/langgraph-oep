# Interrupt Resume With Model Version

Captures approval evidence and model-version drift metadata for an interrupt/resume-style decision.

Key fields: `approval_capture`, `model_alias`, `resolved_model_version`, and `decision_id.drift.model_version`.
