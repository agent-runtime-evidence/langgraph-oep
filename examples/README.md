# Examples

Each example builds a minimal LangGraph graph, attaches `langgraph-oep`, runs inside a `DecisionContext`, and writes deterministic OEP JSON Lines output.

Run from the repository root after installing the package:

```bash
python examples/01_code_review_agent/run.py
```

The examples are compact reference scenarios, not production templates.
